/** Jimma University — shared branding */
export const UNIVERSITY = {
  name: 'Jimma University',
  institute: 'Institute of Technology (JIT)',
  cafeteria: 'Jimma University Student Cafeteria',
  shortCode: 'JU',
};

export function universityTitle() {
  return `${UNIVERSITY.name} — ${UNIVERSITY.institute}`;
}
